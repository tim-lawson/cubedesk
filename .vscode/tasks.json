{
  "tasks": [
    {
      "args": ["animate.py"],
      "command": "${command:python.interpreterPath}",
      "label": "animate",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["fbprophet.py"],
      "command": "${command:python.interpreterPath}",
      "label": "prophet",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["regression.py"],
      "command": "${command:python.interpreterPath}",
      "label": "regression",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["stats.py"],
      "command": "${command:python.interpreterPath}",
      "label": "stats",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    }
  ],
  "version": "2.0.0"
}
